# 验证方式

1. 按 [docs/testing.md](../../docs/testing.md)准备隔离环境和合成数据。
2. 为测试用户建两个标签，用各自标签上传两份不同账单；逐笔查看并按标签过滤。
3. 增删一笔账单的标签，刷新并重新筛选。
4. 重复上传同一文件，确认既有标签不变。
5. 用另一个用户的标签 ID 修改账单，应被拒绝。
6. 对已有任务运行迁移，确认任务标签补齐且人工标签保留。
