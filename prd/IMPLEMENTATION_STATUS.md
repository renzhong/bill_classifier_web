# PRD 未实现部分（待排优先级）

更新时间：2026-09-24。本页只记录各 PRD 尚未实现的部分；PRD 描述目标，现有行为以代码、测试和 `docs/` 为准。下面的编号是讨论用编号，不代表优先级。

| 编号 | 来源 | 尚未实现的部分 |
|---|---|---|
| F1 | PRD 08 | 独立的资产汇总页；目前单月报表已有资产分类与合计，但资产类型只有现金、存款、股票、基金、其他，尚无明确的公司资产类型或自定义类型。 |
| F2 | PRD 08 | 逐笔收入：当前是“月份 + 来源”唯一的一条月度记录，不能记录同月同来源的多笔收入；录入页也没有跨月份的收入总额。具体日期粒度待确认。 |
| F3 | PRD 03/04 | 字典只能维护，尚无读取关键词字典的分类策略类型；精确、正则、跳过、合并、退款等高级策略也未实现，均需逐项确定需求。 |
| F4 | PRD 03 | 修改 Pipeline 后，当前只能逐笔点击“重分类”；没有按筛选条件批量或全量重跑既有账单的入口。 |
| F5 | PRD 06 | 明细页的类别、来源、Tag 仍是单选过滤；尚无虚拟滚动和批量增删 Tag 的页面操作。CSV 导出列在后续里程碑。 |
| F6 | PRD 07 | 分类汇总“看明细”虽传递月份/类别参数，明细页尚未读取这些参数；年度与分类报表的空账户引导不完整，`<300ms` 响应目标尚未验证。 |
| F7 | PRD 04 | Tag 管理页可创建、删除，尚不能修改已有 Tag 的名称和颜色。逐笔账单的 Tag 关联已经可以编辑。 |

PRD 01 的管理员邀请码创建、查看和撤销已完成；PRD 02 的 CSV/XLSX、部分坏行处理和上传 Tag 已完成。PRD 09 的生产 HTTPS、nginx 与部署验收属于待执行的环境验证，不计作待开发功能。类别和 Pipeline 排序当前均使用上下按钮；通用参数表单只支持当前策略的字段类型，这两项按已确认的现状保留。
